import json
from collections import Counter
from pathlib import Path

BASE = Path(__file__).resolve().parent / 'campaign20_six_corpuses_20260905'
RUN = BASE / 'runs/20260905-112254-171550'
def read(p):
    return json.loads(p.read_text(encoding='utf-8'))
manifest = read(BASE/'manifest.json')
findings = read(BASE/'glazing-findings.json')
counts = Counter(f['code'] for f in findings)
repeat = read(BASE/'runs/20260905-113104-025160/results.json')
text = '''# Не годен — аудит агента на 20 проектах

Дата: 05.09.2026. Skill: grp-agent-tester, независимый критик подключён. Проверка контекста MCP: ready=true. Это испытательные графики, не утверждённая выдача для строительства. Производственный код, нормативы и корпоративный MPP-шаблон в рамках аудита не изменялись.

## Итоги

- Ровно 20 исходных проектов, 66 корпусов; каждый построен и проверен дважды.
- Оба прогона: 20/20 проходят сборку, Excel- и IR-валидацию. Изменений IR между прогонами: 0.
- Независимая проверка выявила ошибки состава остекления/кладки в **16 из 20 проектов**: отсутствующие витражи в **14 корпусах**, лишняя наружная кладка в **19 корпусах**.
- Проверены 5 MPP из полных копий корпоративного шаблона. Ошибок штатной проверки 0, автоматический перерасчёт включён; предупреждения не устранены и не являются доказательством корректности.
- На всех 66 корпусах проверенные инварианты фундаментной плиты, подземного монолита и собственного предшественника первого надземного этажа соблюдены. Проверенные длительности положительных объёмов свай соответствуют своему типу и количеству установок.
- Дополнительно выполнены негативные мутации и два однокорпусных воспроизведения. Они не включены в основной счётчик 20 проектов.

## Дефекты графика

### P1 · G01: смешанное остекление теряет витражи

Факт: при ПВХ=true и витражи=true формируется только ПВХ-работа. Сигнатура: `MISSING-STAINED`. Причина: tools/build_grp.py:606 выбирает один прототип, не объединяя требуемые работы; тепловой контур строится по найденным, а не по предусмотренным ТЭП видам остекления (строки 1448, 1527).

Правила: BND-SPK-002/003, BND-TC-001, DEC-33. В valid-004, К3: монолит кровли завершён 23.01.2030; контур (задача 1554) — 08.04.2030, только от ПВХ. Предусмотренные витражи должны завершаться 01.06.2030 (якорь +129 дней). Контур рассчитан **на 54 дня раньше** этого срока. Это оценка конкретной вехи, не доказанное сокращение общего срока проекта на 54 дня.

Минимизация: `minimal-repros/mixed.json` — один корпус, 9 этажей, без подземных этажей и свай. Сборка 0, ошибок IR 0, строк витражей 0 при витражи=true. Дефект не зависит от наличия 3–6 корпусов.

Устранение: формировать состав работ из ТЭП (при смешанном варианте оба вида), затем назначать нормативные связи и даты. В независимом контроле ТЭП→IR требовать полный набор работ и физических предшественников постоянного контура собственного корпуса.

### P1 · G02: при витражах на всю высоту остаётся наружная кладка

Сигнатура: `UNEXPECTED-MASONRY`. BND-SPK-003 прямо исключает наружную кладку при таком остеклении. В valid-003, К1 сохранена кладка длительностью 294 дня, участвующая в связях ВТК. tools/build_grp.py:1454 меняет якорь, но не исключает строку; строки 1504, 1550 продолжают использовать её наличие.

Минимизация: `minimal-repros/full.json` — один корпус, 9 этажей; сборка 0, ошибок IR 0, наружная кладка присутствует при витражи_на_всю_высоту=true.

Устранение: исключать неприменимую работу до сетевого расчёта, адресно переназначать её потребителей по правилам и применять ветку ВТК без кладки. Не удалять строки готового MPP механически; пересобирать график после проверки связей.

## Дефекты агента и валидаторов

### P1 · V01: нет контроля предметной полноты и обязательных связей

Все 20 графиков получают штатный зелёный результат, включая G01/G02. Дополнительно у задачи 1554 в valid-004 удалены все предшественники: IR-валидатор по-прежнему возвращает пустой список ошибок. Воспроизведение: `minimal-repros/missing-contour-predecessors.ir.json`.

Устранение: общий `validate_project_spec` оставить входным шлюзом, но добавить отдельную предметную проверку **ТЭП + IR**: требуемые/запрещённые работы, принадлежность корпусу, полный набор физических предшественников, DEC-34…40. Проверка только выполнимости существующих связей не обнаруживает отсутствующие связи. MPP↔IR не заменяет такой контроль.

### P2 · V02: неверный тип фасада вызывает исключение валидатора

`фасад.тип=[]` и `{}` вызывают `TypeError: unhashable type: 'list'/'dict'` внутри validate_project_spec (tools/build_grp.py:182), вместо списка ошибок. Доказательство — прямой вызов функции; поведение всех внешних обработчиков этим тестом не утверждается. Входы: `facade-list.repro.json`, `facade-object.repro.json`.

Устранение: сначала проверять тип str, только затем принадлежность допустимому набору. Добавить параметризованные тесты null/bool/число/list/dict и требование управляемой ошибки без traceback на всех входных шлюзах.

### P2 · V03: IR пропускает неверную иерархию и даты сводных задач

`wrong-parent.repro.ir.json`: у первого этажа К6 родителем указан первый этаж К1 (обычная задача); ошибок 0. `wrong-summary-dates.repro.ir.json`: начало сводной задачи заменено на 1900-01-01; ошибок 0. Это локальные мутации по одному полю, не дефекты всех исходных графиков.

Устранение: проверять родителя по стеку уровней, его сводный тип и непосредственную вложенность; даты сводной задачи проверять по согласованной модели свёртки дочерних задач. При экспорте учитывать, что Project может пересчитать такую структуру и даты.

### P2 · V04: DEC-40 проверяет повторы, но не полное присутствие разделов

По коду: shared_section_errors проверяет отсутствие разделов только при require_all=true; IR (tools/schedule_ir.py:223) и MPP (tools/mpp_validator.py:253) вызывают её без этого флага. На уровне функции `shared_section_errors([])` возвращает `[]`. Это пробел проверки, а не утверждение, что полностью пустой IR пройдёт все остальные проверки. В основной кампании отсутствие разделов не обнаружено.

Устранение: явный режим полного ГРП с обязательными семью общепроектными разделами, уровнем 1 и единственностью; частичные графики отличать явным контрактом. Добавить проверку в самостоятельную Excel-валидацию.

## Контрольные негативные сценарии

Отрицательное число подземных этажей (−3) и нулевое число установок отклонены управляемо. В основных ТЭП подземные уровни −3…0 представлены количеством 3…0; отрицательное количество не является допустимым входом контракта.

Результаты мутаций: `independent-audit.json`; результаты минимизаций: `minimal-repros/results.json`. Доказательства состава с task_id, датами и предшественниками: `glazing-findings.json`.

## Матрица 20 проектов

Подземные этажи ниже приведены как количества по корпусам. Остекление: П — ПВХ; С — смешанное; В — витражи на всю высоту; Ч — частичные витражи. Сваи: 0 — отсутствуют; 1 — БНС 320/2 установки; 2 — забивные 480/2; 3 — БНС 240/1 + забивные 360/2. Фасад задан на проект, остекление — отдельно на каждый корпус.

| ID | Корпусов | Надземные этажи | Подземные | Фасад | Остекление | Сваи | Выявлено |
|---|---:|---|---|---|---|---:|---|
'''
labels = {'pvc':'П', 'mixed':'С', 'full_stained':'В', 'partial_stained':'Ч'}
for case in manifest['cases']:
    d = case['dimensions']
    codes = sorted({f['code'] for f in findings if f['case'] == case['id']})
    desc = ', '.join({'MISSING-STAINED':'G01','UNEXPECTED-MASONRY':'G02'}[c] for c in codes) or '—'
    text += f"| {case['id']} | {d['corpus_count']} | {','.join(map(str,d['floors']))} | {','.join(map(str,d['underground_by_corpus']))} | {d['facade']} | {','.join(labels[g] for g in d['glazing'])} | {d['pile_profile']} | {desc} |\n"
text += '''
Прочие параметры (отделка, паркинг, ограждение котлована, индивидуальные условия) сохранены в manifest.json и inputs/. Отметка «—» означает отсутствие двух проверенных дефектов, не полную приёмку проекта.

## MPP: стратифицированная выборка

| Проект | Задач | Ошибок | Предупреждений | Автоперерасчёт |
|---|---:|---:|---:|---|
'''
for ident in ['valid-001','valid-002','valid-004','valid-006','valid-018']:
    r = read(RUN/ident/(ident+'.mpp-validation.json'))
    text += f"| {ident} | {r['project']['task_count']} | {r['result']['errors']} | {r['result']['warnings']} | {r['project']['automatic_calculation']} |\n"
text += '''
Предупреждения: открытые начала/окончания задач и, в четырёх случаях, разрывы входа критического пути. Информационные сообщения об охвате связей сводных задач и отчётных вехах не посчитаны как предупреждения. Полные списки — в `<проект>/<проект>.mpp-validation.json` первого прогона. Выборка покрывает 1, 2, 4, 6 корпусов, все фасады и варианты остекления, отсутствие свай и смешанные сваи, подземные границы 0 и 3. Экспорт всех 20 MPP и повторяемость MPP не проверялись.

## Приоритетный план устранения

1. Зафиксировать G01/G02 и V01 как падающие регрессионные тесты на однокорпусных примерах и на смешанных многокорпусных проектах.
2. Исправить формирование состава остекления и исключение кладки; проверить физические связи ВТК/постоянного контура. Не менять нормативы ради прохождения тестов.
3. Ввести независимый validate_project_against_ir(spec, ir) с диагностикой правило/корпус/task_id; вызывать после сборки и перед экспортом MPP.
4. Закрыть V02–V04: безопасные типы входов, иерархия/сводные даты, полнота DEC-40. Общий validate_project_spec не должен заменять проверки уже построенного графика.
5. Повторить эту же матрицу, мутации и MPP-выборку. Критерий: нет G01/G02, каждый намеренно повреждённый IR отклонён конкретной диагностикой, исходные корректные варианты сохраняют проверенные инварианты; предупреждения MPP разобраны отдельно.

## Покрытие и ограничения

Seed 20260905; два запуска одной матрицы дают одинаковые IR. Это воспроизводимость на текущем окружении, не доказательство переносимости. Метаданные и SHA256 входов/контекста находятся в results.json обоих прогонов. Поле agent_policy_version там взято из манифеста контекста (7.5), а пользовательский AGENTS.md сообщает 7.11; нормативные выводы выше опираются на адресно прочитанные правила, не на эту метку.

Для 3–6 корпусов сборщик раскрывает предупреждение о неподтверждённых правилах; испытание расширяет покрытие, но не заменяет их согласование. Произвольные коды корпусов, перестановка их порядка, другие этажности и полное комбинаторное покрытие не проверены. Полный анализ последствий предупреждений MPP и всех нормативных блоков не выполнен. Исправления требуют отдельного запроса.

Файлы воспроизведения и итоговые графики сохранены рядом с отчётом. Первый прогон: runs/20260905-112254-171550; повторный: runs/20260905-113104-025160. Для повторения кампании: `python .agents/skills/grp-agent-tester/scripts/campaign.py run artifacts/campaign20_six_corpuses_20260905` (из корня репозитория; создаёт новый каталог запуска).
'''
assert counts == {'MISSING-STAINED':14, 'UNEXPECTED-MASONRY':19}
assert repeat['passed'] == 20 and repeat['ir_changes_since_previous'] == 0
with (BASE/'AUDIT_REPORT.md').open('x', encoding='utf-8') as f:
    f.write(text)
print(BASE/'AUDIT_REPORT.md')
