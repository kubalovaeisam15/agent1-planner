# Агент 1. Планировщик графика

Рабочая среда агента «Планировщик графика» системы «ИИ-проектный офис» (Level Group).
Пилот — Level Гребной, Этап 1 (выдача в Excel по шаблону ГРП).

> **02.08.2026 — комплект и генератор приведены к текущей редакции `Шаблон_ГРП v2.xlsx`.** Владелец разбил фундаменты и подземный монолит по корпусам и исправил привязку старта отделки. Конвейер собирается, валидируется без ошибок и проходит **все четыре** жёстких критерия приёмки.

## Состав

```
CLAUDE.md                      контракт агента: процесс, выдача, чек-лист §9, границы
РАСХОЖДЕНИЯ_шаблон_v2.md       реестр расхождений шаблона v2 с прежней логикой (R-01…R-44)
instructions/
  typGRP.md                    структура WBS, наименования задач, вехи, регламентные сроки фазы A
  bindings.md                  привязки СМР, якоря, лаги, тепловой контур, сезонность
  standards.md                 числовые нормативы длительностей и умолчания
  Шаблон_ГРП v2.xlsx           эталон структуры: 1 673 задачи (паркинг + 2 корпуса, 1 этап ввода)
tests/
  etalon_TEP.md                вход регрессионного прогона (для человека)
  etalon_project.json          тот же вход в машинном виде — подаётся генератору
  etalon_expected.md           ожидаемый результат и допуски
  template_parsed.json         машинный разбор эталона (генерируется)
tools/
  parse_template.py            разбор шаблона + сверка статистики typGRP.md §3
  build_grp.py                 ГЕНЕРАТОР: проект → Excel для импорта в MS Project
  grp_model.py                 расчётное ядро: нормативы, сезонность, тепловой контур, CPM
  validate_grp.py              валидатор выдачи по машинной части чек-листа §9
  check_regression.py          сверка с эталоном по критериям приёмки §10
  inspect_template.py          разведка структуры xlsx
```

Версии комплекта сверяются между файлами: `CLAUDE.md` v7.4 · `typGRP.md` v4.3 · `bindings.md` v3.2 · `standards.md` v3.3. При расхождении версий агент обязан остановиться (`CLAUDE.md` §0). Единый индекс решений владельца `DEC-*` — `CLAUDE.md` §0.4; реестр непокрытых расхождений шаблона `R-*` — `РАСХОЖДЕНИЯ_шаблон_v2.md`.

## Требования

- Python 3.12+, `openpyxl`, `pytest` (`pip install openpyxl pytest`)
- Git

## Команды

```powershell
# разбор шаблона и сверка со статистикой typGRP.md §3
python tools\parse_template.py

# сборка графика
python tools\build_grp.py tests\etalon_project.json out\ГРП_эталон.xlsx

# валидация выдачи по чек-листу §9
python tools\validate_grp.py out\ГРП_эталон.xlsx

# сверка с эталоном по критериям приёмки §10
python tools\check_regression.py out\ГРП_эталон.xlsx

# юнит-тесты генератора
python -m pytest tests -q
```

## Что изменил шаблон v2

Полный разбор — `РАСХОЖДЕНИЯ_шаблон_v2.md`. Коротко:

**Формат.** Колонки `СДР` больше нет — точечные коды вида `11.4.2.1.8` упразднены. Иерархию несёт только `Уровень структуры`, который теперь подаётся самим шаблоном. Добавлены пустые колонки `Вид работ` и `Код классификатора` — заготовка под справочник МДМ. Строка «Наименование проекта» удалена. Итого 12 колонок вместо 10.

**Структура.** Задачи СМР адресуются префиксом объекта (`П1.`, `К1.`, `К2.`). Появился уровень этапа ввода: «СМР 1-й Этап», «ЗОС и РВЭ → 1-й Этап». Нулевой цикл лежит в блоке паркинга: земляные работы, ограждение и сваи — общие на очередь, **фундаменты и подземный монолит — покорпусные**. Тендерных блоков СМР 48 вместо 53.

**Эталон.** Проект стал двухкорпусным: К1 — 26 этажей, К2 — 55. РС не изменилась (02.04.2024), РВЭ сдвинулось с 22.06.2026 на **31.12.2026**, фаза РС → РВЭ выросла с 811 до **1 003 дней**.

**Отменённые решения владельца.** Шаблон v2 переопределил `DEC-05`, `DEC-06`, `DEC-07`, `DEC-08`, `DEC-10`, `DEC-13`, `DEC-15` и норматив `STD-VIS-001`. Комплект пересчитан по значениям шаблона, каждая позиция помечена статусом 🔴 и требует подтверждения (`РАСХОЖДЕНИЯ_шаблон_v2.md` §7).

**Закрытые вопросы.** Инверсия комментария ВТК исправлена в самом шаблоне; `TC_eff = min(TC_perm, ВТК)` подтверждён поведением К2; финиш ВИС `ЯКОРЬ + 330` (`DEC-23`) подтверждён явным комментарием.

## Конвейер генератора

```
каркас шаблона v2 (1 673 строки)
  → устранение расхождений §13 (перенумерация Ид., пустой блок номинации)
  → наследование связей суммарных строк листьями
  → условия площадки §6.8
  → объекты: корпуса по ТЭП (два прототипа по остеклению), покорпусный нулевой цикл, паркинг
  → нормативы standards.md, поэтажная развёртка монолита
  → сетевой расчёт (проход 1)
  → тепловой контур и отделка по каждому корпусу (bindings.md §3.6, §3.8)
  → сетевой расчёт (проход 2)
  → перенумерация Ид., очистка суммарных строк
  → Excel: ГРП · Обоснование · Допущения · Чек-лист · Сценарии
```

**Адресация без СДР.** Внутренний ключ строки — `Ид.` шаблона (вставленные строки получают `n<N>`); иерархия читается только из «Уровня структуры»; правила `bindings.md` адресуются парой «префикс объекта + наименование» (`К1.` + `По договору Монтаж фасадов`).

## Состояние генератора

Переписан под шаблон v2 01.08.2026, приведён к редакции от 02.08.2026. Собирает 1 677 строк, `validate_grp.py` — **0 ошибок**, регрессия — **все критерии в допуске**.

| Критерий приёмки §10 | Эталон | Расчёт | Откл. | |
| --- | --- | --- | --- | --- |
| Дата РС | 02.04.2024 | 29.03.2024 | −4 дн | ✅ |
| Дата РВЭ | 31.12.2026 | 28.12.2026 | −3 дн | ✅ |
| Фаза A | 447 дн | 443 дн | −4 дн | ✅ |
| Фаза РС → РВЭ | 1 003 дн | 1 004 дн | +1 дн | ✅ |

Ключевая проверка тиражирования пройдена: одно правило даёт разные числа для корпусов — монолит 195 и 369 дн, фундаментная плита 45 и 60 дн, длительность ВИС 410 и 580 дн. `ЯКОРЬ` корпусов расходится на 174 дня, производные финиши считаются каждый от своего.

Проверены краевые ветки: проект без паркинга, один корпус на 40 этажей, три корпуса — все собираются и валидируются без ошибок. **Два и более этапа ввода** генератор не считает намеренно и останавливается с сообщением: правил разнесения объектов, нулевого цикла и благоустройства по этапам владельцем не задано (`typGRP.md` §14 п. 7).

## Открытые позиции

| Позиция | Статус |
| --- | --- |
| Отмена `DEC-05`, `DEC-06`, `DEC-07`, `DEC-08`, `DEC-10`, `DEC-13`, `DEC-15` шаблоном v2 | ⚠ требует подтверждения владельца — `РАСХОЖДЕНИЯ_шаблон_v2.md` §1 |
| Расхождения `R-09`, `R-14`, `R-34`, `R-44` | ✅ закрыты редакцией шаблона от 02.08.2026 |
| Внутренние противоречия шаблона (фасады К2, номинации, финиш ВТК) | ⚠ применены прочтения из колонки «Как применено» — §2 |
| Расхождения шаблона: разрыв `Ид.` 640–644, пустой блок «номинация Арматурный каркас сваи» | ⚠ прежний порядок обхода сохранён — `typGRP.md` §13 |
| Проект без паркинга | ⚠ шаблоном не покрыт: нулевой цикл и ИТП живут в блоке паркинга (`R-05`). Генератор переносит их в первый корпус |
| Правила сценариев | ⏸ не заданы (`DEC-16`); блок сценариев не считается, выдачу не блокирует |
| Справочник МДМ | ⏸ не поступил; колонки «Вид работ» и «Код классификатора» выводятся пустыми |
| Два и более этапа ввода | ⏸ генератор останавливается: правил разнесения объектов по этапам нет (`typGRP.md` §14 п. 7) |
| Промежуточная доля квартир с чистовой отделкой (0 < доля < 1) | ⏸ правилом не покрыта: `standards.md` §15.3 задаёт только полюса. Применяется 270 дн, строка в «Допущения» |
