# Reference only: Python does not automatically load this file.
# Set these variables in the launching shell or cloud environment settings.
AGENT1_RUNTIME=auto
# auto: existing Desktop; CI or missing Desktop: artifacts/output
# local: require existing Desktop; cloud: always artifacts/output
# Optional absolute redirected Desktop; do not put a user-specific value in Git.
# AGENT1_DESKTOP_DIR=
